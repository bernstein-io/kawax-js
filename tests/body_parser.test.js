import BodyParser from '../modules/body_parser';

import Constants from './mocks/constants.mock.js';
import ResourceMock from './mocks/resource.mock.js';

describe('BODY_PARSER', () => {

  it('Action.define return function', () => {
    // let payload = {first_name: 'Walter', last_name: 'Noris'}
    // let schema = ResourceMock.schema;
    // let parser = new BodyParser({schema});
    // let body = parser.process(payload);
  });

});
